## Copy this file to `.env` and fill in values.
##   cp .env.example .env

# Required: Unsplash access key (client-side, Vite will inline it at build time).
VITE_UNSPLASH_ACCESS_KEY=

# Optional: request timeout (ms). Defaults to 10000.
VITE_UNSPLASH_TIMEOUT_MS=10000
